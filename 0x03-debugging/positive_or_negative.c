#include "main.h"
/**
 * positive_or_negative - function to check for posive or negative number
 * @i: number to be checked
 */
void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i);
	else
		printf("%d is positive\n", i);
}
