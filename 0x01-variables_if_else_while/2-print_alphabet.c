#include <stdio.h>
/**
 * main - start of c code
 *
 * Return: 0 program is successful
 */
int main(void)
{
	char alphab;
	for (alphab = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
