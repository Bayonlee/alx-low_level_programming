#include "main.h"
/**
 * print_sign - print sugn function
 * Return: 1 if positive
 * and 0 if zero
 * and -1 if negative
 */
int print_sign(int n)
{
	if (n > 0)
		printf('+');
	return (1);
	else if (n = 0)
		printf('0');
	return (0);
	else
		printf('-');
	return (-1);
}
