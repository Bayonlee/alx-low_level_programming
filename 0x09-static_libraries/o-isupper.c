#include "main.h"
/**
 * _isupper - function to check uppercase letter
 * @a: letternto be checked
 * Return: 1 if uppercase ,else return
 */
int _isupper(int a)
{
	if (a >= 41)
		return (1);
	else
		return (0);
}
